<?php

return [
    /**
     * Default UUID column
     *
     * Every model that uses the UUID for scope, should have a UUID column to
     * reference the UUID value.
     */
    'uuid_column' => 'uuid',
];
